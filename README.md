# Dropbox-Backup-Scheduler-API
An api that creates automated backup of specific mysql tables, whole database and specified folders then upload them on dropbox according to scheduled time. A great tool for backing up data regularly, should be used in servers to keep regular backups of different project assets.
